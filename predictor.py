# class TrajectoryPredictor:

#     def __init__(self, model_path):
#         ...

#     def predict(self, marker_id):

#         history = history_manager.get_window(marker_id)

#         if history is None:
#             return None

#         return model(history)